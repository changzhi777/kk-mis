fastapi>=0.110
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
aiosqlite>=0.20
redis>=5.0
pydantic>=2.6
python-multipart>=0.0.9
pyjwt>=2.8
bcrypt>=4.0
httpx>=0.28  # OAuth 第三方登录（GitHub/微信 API 调用）

# ── Storage Sprint 1：腾讯云 COS（可选；backend=cos 时启用）──
# Sprint 0 默认 local backend，无需此依赖；切 cos 后 `pip install cos-python-sdk-v5`
cos-python-sdk-v5>=1.9.0   # 用于 cos-python-sdk-v5 COS S3-兼容客户端
prometheus_client>=0.20

# === P0 CMS 真支付 2026-07-15 ===
alembic>=1.13.0
# 微信支付 v3 验签/解密（生产用）
wechatpayv3>=1.0.0      # 微信支付 v3 SDK（RSA + AES-GCM）
pycryptodome>=3.18.0    # AES-256-GCM 解密（兜底 SDK 缺）
cryptography>=41.0.0    # RSA 验签 + 平台证书管理

# === Office Engine 本地引擎（2026-07-16）===
# admin app/services/office/engine.py 依赖
# 缺包时 engine.py 内部抛 OfficeEngineError（fail-closed），不影响 admin 启动
openpyxl>=3.1           # data_to_excel (xlsx 生成)
python-pptx>=0.6         # data_to_pptx (pptx 生成)
# weasyprint>=60.0       # html_to_pdf (需要 cairo/pango 系统库；macOS brew install pango libffi)
# reportlab>=4.0         # TripGen PDF body（lazy import）
